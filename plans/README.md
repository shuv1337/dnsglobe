# Implementation Plans

Generated by the improve skill on 2026-07-05, against commit `c298021`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

Audit context: full direct audit of all 5 source files (1,187 lines), read
line-by-line by the advisor. The panic in plan 002 was confirmed with a
standalone repro; the migration in plan 003 was compile- and runtime-verified
in a scratch copy (build clean, 5/5 tests, live query OK, `cargo audit`
clean afterward).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | CI gate: fmt + clippy + test workflow          | P1 | S | —        | TODO |
| 002  | Non-ASCII CLI domain panic fix                 | P1 | S | 001 (soft) | TODO |
| 003  | hickory 0.26 upgrade (RUSTSEC-2026-0119) + ratatui 0.30 | P1 | M | 001, 002 | TODO |
| 004  | Watch-mode consensus on deleted records        | P2 | S | 001 (soft) | TODO |
| 005  | --help / --version flags                       | P2 | S | 002      | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- **001 first**: it establishes the fmt/clippy/test gates every later plan
  uses as done criteria, and applies the one-time `cargo fmt` so later
  diffs stay clean.
- **002 before 003**: plan 003's replacement `src/dns.rs` already contains
  002's `short_error` fix; if 003 runs first, mark 002's Step 3 as
  done-by-003 in its report.
- **002 before 005**: both edit `main()`'s argument handling; 005's drift
  check assumes 002's `--once` validation is present.
- **003 and 004 are independent** of each other (different files).

## Direction findings (not planned — maintainer's call)

Documented in the audit; each would start as a design/spike plan on request:

- **D-01** `--json` output for `--once` (grounded: `--once` is explicitly
  "for scripts" — src/main.rs:28, README).
- **D-02** Runtime resolver config, e.g. `--resolvers <file>` (grounded:
  README invites editing `src/resolvers.rs`, impossible for Homebrew/binary
  installs without recompiling).
- **D-03** DoH/DoT transports (grounded: `src/resolvers.rs:6-8` documents
  the Africa coverage gap caused by Do53 refusal; hickory 0.26 — plan 003 —
  ships these behind feature flags). Sequence after 003.

## Findings considered and rejected

- **`summary()` recomputed every 100 ms draw tick**: O(34) union-find with
  small HashMaps — unmeasurable at this scale; caching would add state and
  invalidation for zero user-visible gain.
- **Standalone clippy-warning fix**: folded into plan 001 (it's the gate's
  prerequisite, not its own plan).
- **Outer-timeout TCP-fallback preemption (F-06)**: folded into plan 003 —
  same file is fully rewritten there.
- **`short_error` byte-truncate panic (F-01b)**: folded into plan 002 (same
  bug class as the headline panic).
- **README privacy note** (every typed domain goes to 34 third-party
  resolvers incl. CN/RU operators): informational; inherent to the tool's
  purpose. One sentence in README if the maintainer wants it — not worth a
  plan.
- **`lru` unsound-advisory warning (RUSTSEC-2026-0002)**: transitive via
  ratatui; warning-severity only, likely resolved upstream — re-check after
  plan 003.
